import { defineConfig } from '@prisma/config';

export default defineConfig({
  schema: './prisma/schema.prisma', // chemin vers ton schema
  datasource: {
    url: process.env.DATABASE_URL!, // <- Ici on passe la DB
  },
});
